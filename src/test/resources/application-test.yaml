spring:
  datasource:
    url: jdbc:h2:mem:hotel-info-test-db
    driver-class-name: org.h2.Driver
    username: user
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: none
  liquibase:
    change-log: classpath:/db/changelog/db.changelog-test.yaml